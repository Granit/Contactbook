<%= javascript_include_tag "prototype" %>

  <%= collection_select :id, :id, @new_tags, :id, :name, 
  {:include_blank => true}, 
  {:onchange => remote_function(:url => {:action => 'add_tag'},
  :with => "'id=' + this.value + '&old_tags='",
  :update => "mydiv1")} %>

